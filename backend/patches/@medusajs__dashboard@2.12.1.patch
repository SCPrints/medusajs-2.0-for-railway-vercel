diff --git a/dist/chunk-IQBAUTU5.mjs b/dist/chunk-IQBAUTU5.mjs
index e3a72ee54ee1c53baaeae0ff7b3218352eacdba9..d0226b8e6c797794604bfe9ca60c0e6661d7f952 100644
--- a/dist/chunk-IQBAUTU5.mjs
+++ b/dist/chunk-IQBAUTU5.mjs
@@ -6,6 +6,9 @@ import {
 import { useTranslation } from "react-i18next";
 import { jsx, jsxs } from "react/jsx-runtime";
 var ProductCell = ({ product }) => {
+  if (!product || typeof product !== "object") {
+    return /* @__PURE__ */ jsx("div", { className: "flex h-full w-full max-w-[250px] items-center gap-x-3 overflow-hidden", children: /* @__PURE__ */ jsx("span", { className: "truncate text-ui-fg-muted", children: "—" }) });
+  }
   return /* @__PURE__ */ jsxs("div", { className: "flex h-full w-full max-w-[250px] items-center gap-x-3 overflow-hidden", children: [
     /* @__PURE__ */ jsx("div", { className: "w-fit flex-shrink-0", children: /* @__PURE__ */ jsx(Thumbnail, { src: product.thumbnail }) }),
     /* @__PURE__ */ jsx("span", { title: product.title, className: "truncate", children: product.title })
diff --git a/dist/chunk-PCFUZKDS.mjs b/dist/chunk-PCFUZKDS.mjs
index 0def99505968fea5ec0b228dfab075e9a8b2ac0f..9b022372bf35ae9320d7ea92c370a28c6b4878d4 100644
--- a/dist/chunk-PCFUZKDS.mjs
+++ b/dist/chunk-PCFUZKDS.mjs
@@ -3,6 +3,14 @@ import {
 } from "./chunk-C76H5USB.mjs";
 
 // src/hooks/table/query/use-product-table-query.tsx
+var safeJsonParse = (raw) => {
+  if (!raw) return void 0;
+  try {
+    return JSON.parse(raw);
+  } catch {
+    return void 0;
+  }
+};
 var DEFAULT_FIELDS = "id,title,handle,status,*collection,*sales_channels,variants.id,thumbnail";
 var useProductTableQuery = ({
   prefix,
@@ -44,8 +52,8 @@ var useProductTableQuery = ({
     limit: pageSize,
     offset: offset ? Number(offset) : 0,
     sales_channel_id: sales_channel_id?.split(","),
-    created_at: created_at ? JSON.parse(created_at) : void 0,
-    updated_at: updated_at ? JSON.parse(updated_at) : void 0,
+    created_at: safeJsonParse(created_at),
+    updated_at: safeJsonParse(updated_at),
     category_id: category_id?.split(","),
     collection_id: collection_id?.split(","),
     is_giftcard: is_giftcard ? is_giftcard === "true" : void 0,
diff --git a/dist/chunk-XLDQPLK4.mjs b/dist/chunk-XLDQPLK4.mjs
index e4760559b694c9c9a1c831304cdf84ce4add2bee..92dc9271e00df6d4ddc33527438276d97d0ffa5c 100644
--- a/dist/chunk-XLDQPLK4.mjs
+++ b/dist/chunk-XLDQPLK4.mjs
@@ -9,10 +9,14 @@ import {
 import { useTranslation } from "react-i18next";
 import { jsx } from "react/jsx-runtime";
 var CollectionCell = ({ collection }) => {
-  if (!collection) {
+  if (!collection || typeof collection !== "object" || Array.isArray(collection)) {
     return /* @__PURE__ */ jsx(PlaceholderCell, {});
   }
-  return /* @__PURE__ */ jsx("div", { className: "flex h-full w-full items-center overflow-hidden", children: /* @__PURE__ */ jsx("span", { className: "truncate", children: collection.title }) });
+  const collTitle = collection.title;
+  if (collTitle == null || collTitle === "") {
+    return /* @__PURE__ */ jsx(PlaceholderCell, {});
+  }
+  return /* @__PURE__ */ jsx("div", { className: "flex h-full w-full items-center overflow-hidden", children: /* @__PURE__ */ jsx("span", { className: "truncate", children: collTitle }) });
 };
 var CollectionHeader = () => {
   const { t } = useTranslation();
@@ -24,12 +28,15 @@ import { useTranslation as useTranslation2 } from "react-i18next";
 import { jsx as jsx2 } from "react/jsx-runtime";
 var ProductStatusCell = ({ status }) => {
   const { t } = useTranslation2();
-  const [color, text] = {
+  const mapping = {
     draft: ["grey", t("products.productStatus.draft")],
     proposed: ["orange", t("products.productStatus.proposed")],
     published: ["green", t("products.productStatus.published")],
     rejected: ["red", t("products.productStatus.rejected")]
-  }[status];
+  };
+  const entry = mapping[status];
+  const color = entry?.[0] ?? "grey";
+  const text = entry?.[1] ?? (status != null && status !== "" ? String(status) : "—");
   return /* @__PURE__ */ jsx2(StatusCell, { color, children: text });
 };
 var ProductStatusHeader = () => {
@@ -45,25 +52,28 @@ var SalesChannelsCell = ({
   salesChannels
 }) => {
   const { t } = useTranslation3();
-  if (!salesChannels || !salesChannels.length) {
+  const raw = Array.isArray(salesChannels) ? salesChannels : [];
+  const channels = raw.filter((sc) => sc != null && typeof sc === "object");
+  if (!channels.length) {
     return /* @__PURE__ */ jsx3(PlaceholderCell, {});
   }
-  if (salesChannels.length > 2) {
+  const nameOf = (sc) => String(sc.name ?? "");
+  if (channels.length > 2) {
     return /* @__PURE__ */ jsxs("div", { className: "flex h-full w-full items-center gap-x-1 overflow-hidden", children: [
-      /* @__PURE__ */ jsx3("span", { className: "truncate", children: salesChannels.slice(0, 2).map((sc) => sc.name).join(", ") }),
+      /* @__PURE__ */ jsx3("span", { className: "truncate", children: channels.slice(0, 2).map((sc) => nameOf(sc)).join(", ") }),
       /* @__PURE__ */ jsx3(
         Tooltip,
         {
-          content: /* @__PURE__ */ jsx3("ul", { children: salesChannels.slice(2).map((sc) => /* @__PURE__ */ jsx3("li", { children: sc.name }, sc.id)) }),
+          content: /* @__PURE__ */ jsx3("ul", { children: channels.slice(2).map((sc, i) => /* @__PURE__ */ jsx3("li", { children: nameOf(sc) }, sc.id ?? `sc-${i}`)) }),
           children: /* @__PURE__ */ jsx3("span", { className: "text-xs", children: t("general.plusCountMore", {
-            count: salesChannels.length - 2
+            count: channels.length - 2
           }) })
         }
       )
     ] });
   }
-  const channels = salesChannels.map((sc) => sc.name).join(", ");
-  return /* @__PURE__ */ jsx3("div", { className: "flex h-full w-full items-center overflow-hidden max-w-[250px]", children: /* @__PURE__ */ jsx3("span", { title: channels, className: "truncate", children: channels }) });
+  const channelNames = channels.map((sc) => nameOf(sc)).join(", ");
+  return /* @__PURE__ */ jsx3("div", { className: "flex h-full w-full items-center overflow-hidden max-w-[250px]", children: /* @__PURE__ */ jsx3("span", { title: channelNames, className: "truncate", children: channelNames }) });
 };
 var SalesChannelHeader = () => {
   const { t } = useTranslation3();
@@ -75,10 +85,11 @@ import { useTranslation as useTranslation4 } from "react-i18next";
 import { jsx as jsx4 } from "react/jsx-runtime";
 var VariantCell = ({ variants }) => {
   const { t } = useTranslation4();
-  if (!variants || !variants.length) {
+  const variantList = Array.isArray(variants) ? variants : [];
+  if (!variantList.length) {
     return /* @__PURE__ */ jsx4(PlaceholderCell, {});
   }
-  return /* @__PURE__ */ jsx4("div", { className: "flex h-full w-full items-center overflow-hidden", children: /* @__PURE__ */ jsx4("span", { className: "truncate", children: t("products.variantCount", { count: variants.length }) }) });
+  return /* @__PURE__ */ jsx4("div", { className: "flex h-full w-full items-center overflow-hidden", children: /* @__PURE__ */ jsx4("span", { className: "truncate", children: t("products.variantCount", { count: variantList.length }) }) });
 };
 var VariantHeader = () => {
   const { t } = useTranslation4();
diff --git a/dist/product-detail-ATPFDPI2.mjs b/dist/product-detail-ATPFDPI2.mjs
index a2be9ca8caa13af689f54f40bcb05a257aa998d8..cfa439818003e04437c2ef43d7c30c2aa538cdc1 100644
--- a/dist/product-detail-ATPFDPI2.mjs
+++ b/dist/product-detail-ATPFDPI2.mjs
@@ -647,7 +647,7 @@ var ProductOrganizationSection = ({
       SectionRow,
       {
         title: t("fields.categories"),
-        value: product.categories?.length ? product.categories.map((pcat) => /* @__PURE__ */ jsx6(
+        value: product.categories?.length ? product.categories.filter((pcat) => pcat != null && typeof pcat === "object").map((pcat) => /* @__PURE__ */ jsx6(
           OrganizationTag,
           {
             label: pcat.name,
@@ -676,10 +676,11 @@ var ProductSalesChannelSection = ({
 }) => {
   const { count } = useSalesChannels();
   const { t } = useTranslation6();
-  const availableInSalesChannels = product.sales_channels?.map((sc) => ({
+  const rawSc = Array.isArray(product.sales_channels) ? product.sales_channels : [];
+  const availableInSalesChannels = rawSc.filter((sc) => sc != null && typeof sc === "object").map((sc) => ({
     id: sc.id,
-    name: sc.name
-  })) ?? [];
+    name: String(sc.name ?? "")
+  }));
   const firstChannels = availableInSalesChannels.slice(0, 3);
   const restChannels = availableInSalesChannels.slice(3);
   return /* @__PURE__ */ jsxs6(Container6, { className: "flex flex-col gap-y-4 px-6 py-4", children: [
@@ -709,7 +710,7 @@ var ProductSalesChannelSection = ({
         restChannels.length > 0 && /* @__PURE__ */ jsx7(
           Tooltip3,
           {
-            content: /* @__PURE__ */ jsx7("ul", { children: restChannels.map((sc) => /* @__PURE__ */ jsx7("li", { children: sc.name }, sc.id)) }),
+            content: /* @__PURE__ */ jsx7("ul", { children: restChannels.map((sc, i) => /* @__PURE__ */ jsx7("li", { children: sc.name }, sc.id ?? `sc-${i}`)) }),
             children: /* @__PURE__ */ jsx7(
               Text2,
               {
diff --git a/dist/product-sales-channels-5YIO22UW.mjs b/dist/product-sales-channels-5YIO22UW.mjs
index 25cb1b6f27765004eebc31a393b17b97c81828a3..5ad2f4c33503a9b535db27ea35da60b7beccfb4b 100644
--- a/dist/product-sales-channels-5YIO22UW.mjs
+++ b/dist/product-sales-channels-5YIO22UW.mjs
@@ -56,17 +56,18 @@ var EditSalesChannelsForm = ({
 }) => {
   const { t } = useTranslation();
   const { handleSuccess } = useRouteModal();
+  const safeSalesChannels = (product.sales_channels ?? []).filter((sc) => sc != null && typeof sc === "object" && sc.id != null);
   const form = useForm({
     defaultValues: {
-      sales_channels: product.sales_channels?.map((sc) => sc.id) ?? []
+      sales_channels: safeSalesChannels.map((sc) => sc.id)
     },
     resolver: zodResolver(EditSalesChannelsSchema)
   });
   const { setValue } = form;
-  const initialState = product.sales_channels?.reduce((acc, curr) => {
+  const initialState = safeSalesChannels.reduce((acc, curr) => {
     acc[curr.id] = true;
     return acc;
-  }, {}) ?? {};
+  }, {});
   const [rowSelection, setRowSelection] = useState(initialState);
   useEffect(() => {
     const ids = Object.keys(rowSelection);
