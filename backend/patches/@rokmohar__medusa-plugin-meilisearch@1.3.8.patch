diff --git a/.medusa/server/src/jobs/meilisearch-products-index.js b/.medusa/server/src/jobs/meilisearch-products-index.js
index 872112ffeb63bc9007f3fe3725551ed166703e1f..fbb247f4aca869d61b43678d255222807e05e082 100644
--- a/.medusa/server/src/jobs/meilisearch-products-index.js
+++ b/.medusa/server/src/jobs/meilisearch-products-index.js
@@ -11,7 +11,9 @@ const sync_products_1 = require("../workflows/sync-products");
  */
 const productsIndexJobWorkflow = (0, workflows_sdk_1.createWorkflow)('products-index-job-workflow', () => {
     const result = sync_products_1.syncProductsWorkflow.runAsStep({
-        input: {},
+        // SC Prints patch: smaller batches => lighter per-pass query.graph load
+        // (default 1000 pulls all products + price/variant/tag/brand joins at once).
+        input: { batchSize: 200 },
     });
     return new workflows_sdk_1.WorkflowResponse(result);
 });
@@ -23,7 +25,14 @@ async function meilisearchProductsIndexJob(container) {
 }
 exports.config = {
     name: 'meilisearch-products-index',
-    schedule: '* * * * *',
+    // SC Prints patch: upstream default is '* * * * *' (every minute). A full
+    // reindex of our ~1400-product catalog takes ~6 min, so per-minute runs
+    // pile up and pin CPU (BullMQ "Missing lock ... moveToFinished" loop).
+    // Real-time product-upsert/-delete subscribers keep search fresh; this
+    // scheduled pass is only a drift-healing reconcile, so once a day at
+    // 16:00 UTC (02:00 AEST, off-peak) is plenty. Re-create this patch on
+    // plugin upgrade (see Docs/ upgrade runbook).
+    schedule: '0 16 * * *',
     numberOfExecutions: 1,
 };
 //# sourceMappingURL=data:application/json;base64,
\ No newline at end of file
diff --git a/.medusa/server/src/workflows/steps/sync-products.js b/.medusa/server/src/workflows/steps/sync-products.js
index 14ecc0a6f567f6e8da26812d52cd7fe744cfaf72..f38dfe6603925f403ae3657ed6afd10106641e6f 100644
--- a/.medusa/server/src/workflows/steps/sync-products.js
+++ b/.medusa/server/src/workflows/steps/sync-products.js
@@ -38,6 +38,7 @@ exports.syncProductsStep = (0, workflows_sdk_1.createStep)('sync-products', asyn
     }
     const validProductIds = new Set(allProductIds);
     const productsToDelete = new Set();
+    let indexedSeenCount = 0;
     for (const index of productIndexes) {
         let indexOffset = 0;
         let hasMoreIndexed = true;
@@ -53,6 +54,7 @@ exports.syncProductsStep = (0, workflows_sdk_1.createStep)('sync-products', asyn
                 hasMoreIndexed = false;
                 break;
             }
+            indexedSeenCount += indexedResult.hits.length;
             indexedResult.hits.forEach((hit) => {
                 if (!validProductIds.has(hit.id)) {
                     productsToDelete.add(hit.id);
@@ -64,6 +66,28 @@ exports.syncProductsStep = (0, workflows_sdk_1.createStep)('sync-products', asyn
             }
         }
     }
+    // SC Prints anti-gutting guard (2026-06-18 incident). This step deletes
+    // every indexed doc not present in `validProductIds`. If the heavy product
+    // graph query above truncated (timeout under DB load / colliding with the
+    // 03:00-06:00 UTC sync + tier-regen pile-up), `validProductIds` is tiny and
+    // `productsToDelete` is mostly REAL products. On 2026-06-02..05 a fetch
+    // returning ~9 of ~1352 products drove this to delete ~1000/day with a
+    // SUCCESS status, draining the prod index to ~105. Refuse to delete when
+    // the fetched set is implausibly small vs what's indexed — log + skip so a
+    // human reruns (or the next clean pass self-heals) rather than lose search.
+    const validCount = validProductIds.size;
+    const wouldGutIndex = productsToDelete.size > 0 &&
+        (validCount < 10 ||
+            (indexedSeenCount > 0 && validCount < indexedSeenCount * 0.5));
+    if (wouldGutIndex) {
+        const logger = container.resolve('logger');
+        logger.error(`[meilisearch sync-products] ABORTING orphan deletion: only ${validCount} valid products fetched but the index holds ~${indexedSeenCount}; would have deleted ${productsToDelete.size}. The source query likely truncated — refusing to gut the index.`);
+        return new workflows_sdk_1.StepResponse({
+            totalProcessed: allProductIds.length,
+            totalDeleted: 0,
+            skippedDeletion: true,
+        });
+    }
     if (productsToDelete.size > 0) {
         const orphanedIds = Array.from(productsToDelete);
         for (let i = 0; i < orphanedIds.length; i += batchSize) {
