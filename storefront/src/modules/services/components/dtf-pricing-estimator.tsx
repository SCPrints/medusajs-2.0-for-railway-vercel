"use client"

/**
 * Compatibility shim:
 * keeps the old module path resolvable for dev-server caches/import traces.
 * This component intentionally renders nothing.
 */
export default function DtfPricingEstimator() {
  return null
}
